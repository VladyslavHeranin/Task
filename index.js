let string = 'cool';

let subString = 'o';

function srt(string, subString) {
    result = string.indexOf(subString);
    if (result === -1) {
        console.log(false)
    } else {
        console.log(true)
    }
}
 